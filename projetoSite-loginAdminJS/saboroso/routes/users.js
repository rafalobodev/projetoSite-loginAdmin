//var conn = require('./../inc/db');//faz require da conexão db q ta dentro da pasta inc
//var express = require('express');//require express e router padrão
//var router = express.Router();

/* GET users listing. */
//router.get('/', function(req, res, next) {//se responder com esse require na url localhost/users...,,
  //res.send('respond with a resource');//sai tela branca com essa string apenas,, saida aqui é sem sql conectado

  //conn.query("SELECT * FROM tb_users ORDER BY name", (err, results) => {//(retornando erro,resultados campos, parametros)..com a require linha 1 é possivel fazer uma conn pegando codigo de https://www.npmjs.com/package/mysql2

  //  if(err){//se tiver algum erro..
  //    res.send(err);//reponde oq foi o erro
 //   } else {
 //     res.send(results);//se não se deu tudo certo resultados...
//    }
//  });
//});

//module.exports = router;//necessario para conexão do sql
